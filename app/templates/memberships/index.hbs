<h1>My Memberships</h1>
<div class="row">
    <div class="col-md-6">
        <h2>Wwoofer Memberships</h2>
        {{#if wwoofMemberships}}
            <table class="table table-hover">
                <thead>
                <tr>
                    <th>Code</th>
                    <th>Payment</th>
                    <th>Amount</th>
                    <th>Expire At</th>
                </tr>
                </thead>
                <tbody>
                {{#each wwoofMemberships}}
                    <tr>
                        <td>{{itemCode}}</td>
                        <td>{{format-date createdAt}}</td>
                        <td>{{total}} Euro</td>
                        <td>{{format-date expireAt}}</td>
                    </tr>
                {{/each}}
                </tbody>
            </table>
            <p>
                You may renew your membership 30 days before it expires.
                <button {{action "renewWwoofMembership"}} {{bind-attr disabled="wwoofCanRenewMembership"}}
                        class="btn btn-primary">Renew Membership
                </button>
            </p>
        {{else}}
            You've never taken a membership as a Wwoofer.
        {{/if}}
    </div>
    <div class="col-md-6">
        <h2>Host Memberships</h2>
        {{#if hostMemberships}}
            <table class="table table-hover">
                <thead>
                <tr>
                    <th>Code</th>
                    <th>Payment</th>
                    <th>Amount</th>
                    <th>Expire At</th>
                </tr>
                </thead>
                <tbody>
                {{#each hostMemberships}}
                    <tr>
                        <td>{{itemCode}}</td>
                        <td>{{format-date createdAt}}</td>
                        <td>{{total}} Euro</td>
                        <td>{{format-date expireAt}}</td>
                    </tr>
                {{/each}}
                </tbody>
            </table>
            <p>
                You may renew your membership 30 days before it expires.
                <button {{action "renewHostMembership"}} {{bind-attr disabled="hostCannotRenewMembership"}}
                        class="btn btn-primary">Renew Membership
                </button>
            </p>
        {{else}}
            You've never taken a membership as a Host.
        {{/if}}
    </div>
</div>