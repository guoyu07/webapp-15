{{#if isPending}}
    {{#if controllers.application.currentUserIsAdmin}}
        <div class="alert alert-info">
            <span class="glyphicon glyphicon-info-sign"></span>
            This host profile is pending for validation.
            Review its details below then
            <a href="#" {{action "approveHost"}} class="alert-link">approve it</a>.
        </div>
    {{else}}
        <div class="alert alert-success alert-dismissable" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            <span class="glyphicon glyphicon-ok-sign"></span>
            Your host profile was succesfully created.
            We must validate your profile before it gets published.
            In the meantime you can upload pictures of your farm at the bottom of the page.
        </div>
    {{/if}}
{{/if}}