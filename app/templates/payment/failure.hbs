<div class="page-header">
    <h1>{{t 'payment.failure.title'}}</h1>
</div>

<p>{{t 'payment.failure.instructions'}} <strong>{{reason}}</strong></p>

<p>
    {{t 'payment.failure.retry'}}
    {{t 'payment.failure.contact'}}
</p>

{{#link-to 'memberships.new' (query-params type=membershipType) class="btn btn-primary"}}{{t 'literal.retry'}}{{/link-to}}
