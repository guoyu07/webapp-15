<div class="row">
    <div class="col-sm-3">
        <ul class="sidenav-list">
            <li class="sidenav-item">{{#link-to 'host.edit' model.id}}{{t 'literal.editProfile'}}{{/link-to}}</li>
            <li class="sidenav-item">{{#link-to 'host.photos' model.id}}{{t 'literal.photos'}}{{/link-to}}</li>
        </ul>
        {{#link-to 'host.index' model.id class="btn btn-primary btn-block mtm mbm"}}{{t 'literal.viewProfile'}}{{/link-to}}
    </div>
    <div class="col-sm-9">
        {{outlet}}
    </div>
</div>
