<div class="row">
  <div class="col-xs-12 col-sm-9">
    <form class="form-horizontal">
      {{review-form
          review=review
          validations=validations}}

      <div class="form-group">
        <div class="col-xs-12">
          <button type="button" class="btn btn-primary pull-right" onclick={{action 'submitReview' review}} disabled={{review.isSaving}}>
            {{t 'reviews.new.submitReview'}}
          </button>
        </div>
      </div>
    </form>
  </div>
  <div class="hidden-xs col-sm-3">
    {{help-articles label='review-new'}}
  </div>
</div>
