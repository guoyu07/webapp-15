{{#each option in options}}
    <label {{bind-attr for=option.value}} class="checkbox checkbox-inline">
        <input type="checkbox" {{bind-attr id=option.value}} {{bind-attr checked=option.isSelected}} {{action "select" option on="change"}}>
        {{option.label}}
    </label>
{{/each}}
