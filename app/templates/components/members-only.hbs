{{#if session.isAuthenticated}}
  {{#if sessionUser.user.wwoofer}}
    {{#link-to 'memberships.new' (query-params type='W' itemCode='WO1')}}
      {{t 'host.index.membersOnly'}}
    {{/link-to}}
  {{else}}
    <i>{{t 'host.index.membersOnly'}}</i>
  {{/if}}
{{else}}
  {{#link-to 'users.new'}}{{t 'host.index.membersOnly'}}{{/link-to}}
{{/if}}
